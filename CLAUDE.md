## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `ninefyi/openscad_web_management`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
